const Interface = ({ children }) => {
      return (
          <section className="phone-case">
                <div className="phone-screen">{children}</div>
                    </section>
                      );
                      }
                      export default Interface;
}